<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/container"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background"
        android:fitsSystemWindows="true">
    <FrameLayout
            android:id="@+id/fragment_holder"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="11">
        <ProgressBar
                android:id="@+id/progressBar"
                android:layout_width="100dp" android:layout_height="100dp"
                android:layout_gravity="center"
                android:indeterminateTint="@color/colorAccentGreen"/>
    </FrameLayout>
    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/nav_view"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_gravity="bottom"
            android:layout_height="0dp"
            app:itemBackground="@color/colorPrimaryDark"
            android:background="@color/colorPrimaryDark"
            android:outlineSpotShadowColor="@color/colorTransparent"
            android:outlineAmbientShadowColor="@color/colorTransparent"
            app:itemIconTint="@drawable/bottom_nav_workout_selected"
            app:itemTextColor="@drawable/bottom_nav_workout_selected"
            app:menu="@menu/bottom_nav_menu"/>
</LinearLayout>